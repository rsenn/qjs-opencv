# Vector-argument conversion sites

Catalog of every `js_array_(to|from)`/`js_value_(to|from)` call in `js_*.cpp` that marshals a JS argument (`argv[N]`) into or out of one of the container types aliased in `include/types.hpp` (`CharVector`, `DMatchVector`, `DoubleVector`, `FloatVector`, `IntVector`, `KeyPointVector`, `MatVector`, `Point2fVector`, `Point3fVector`, `PointVector`, `RectVector`, `StringVector` and their `*VectorVector` nested forms) - as opposed to a single scalar/struct value (`cv::Point`, `cv::Rect`, `double`, ...), which `js_value_to`/`js_array_to` also handle but which aren't in scope here.

Generated by matching `std::vector<T>` (or nested `std::vector<std::vector<T>>`) variable declarations against the immediately following `js_(array|value)_(to|from)(ctx, argv[...], var)` call sites across `js_*.cpp`. One commented-out call (`js_dnn.cpp` `DNN_NMSBOXES`, `// js_value_to(ctx, argv[4], indices)`) is intentionally excluded from the 43 sites below - `indices` is an output-only parameter there, filled by `cv::dnn::NMSBoxes()` itself rather than read from an argument.

## `CharVector` (`std::vector<char>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `CharVectorVector` (`std::vector<std::vector<char>>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `DMatchVector` (`std::vector<cv::DMatch>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `DMatchVectorVector` (`std::vector<std::vector<cv::DMatch>>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `DoubleVector` (`std::vector<double>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_cv.cpp` | 203 | `js_array_to` | `argv[3]` | `values` |
| `js_cv.cpp` | 1159 | `js_array_to` | `argv[3]` | `crit` |
| `js_imgproc.cpp` | 863 | `js_array_to` | `argv[6]` | `value` |
| `js_imgproc.cpp` | 908 | `js_array_to` | `argv[7]` | `value` |
| `js_imgproc.cpp` | 2397 | `js_array_to` | `argv[2]` | `crit` |

## `FloatVector` (`std::vector<float>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_dnn.cpp` | 2275 | `js_value_to` | `argv[1]` | `scores` |
| `js_dnn.cpp` | 2604 | `js_array_to` | `argv[1]` | `scores` |
| `js_dnn.cpp` | 2642 | `js_value_to` | `argv[1]` | `scores` |
| `js_feature2d.cpp` | 436 | `js_array_to` | `argv[0]` | `radiusList` |
| `js_feature2d.cpp` | 452 | `js_array_to` | `argv[2]` | `radiusList` |

## `IntVector` (`std::vector<int>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_cv.cpp` | 93 | `js_array_to` | `argv[3]` | `params` |
| `js_cv.cpp` | 143 | `js_array_to` | `argv[3]` | `delays` |
| `js_cv.cpp` | 451 | `js_array_to` | `argv[i++]` | `fromTo` |
| `js_cv.cpp` | 1013 | `js_value_to` | `argv[1]` | `order` |
| `js_dnn.cpp` | 1210 | `js_value_to` | `argv[0]` | `tokens` |
| `js_dnn.cpp` | 2276 | `js_value_to` | `argv[2]` | `class_ids` |
| `js_dnn.cpp` | 2610 | `js_array_to` | `argv[5]` | `indices` |
| `js_feature2d.cpp` | 777 | `js_array_to` | `argv[4]` | `selectedPairs` |
| `js_imgproc.cpp` | 798 | `js_array_to` | `argv[1]` | `channels` |
| `js_mat.cpp` | 319 | `js_array_to` | `argv[index]` | `sizes` |
| `js_mat.cpp` | 388 | `js_array_to` | `argv[1]` | `sizes` |
| `js_mat.cpp` | 407 | `js_array_to` | `argv[0]` | `sizes` |
| `js_mat.cpp` | 1696 | `js_array_to` | `argv[0]` | `newshape` |
| `js_umat.cpp` | 847 | `js_array_to` | `argv[0]` | `newshape` |
| `js_video_capture.cpp` | 51 | `js_array_to` | `argv[2]` | `params` |

## `KeyPointVector` (`std::vector<cv::KeyPoint>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `KeyPointVectorVector` (`std::vector<std::vector<cv::KeyPoint>>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `MatVector` (`std::vector<cv::Mat>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_cv.cpp` | 139 | `js_array_to` | `argv[1]` | `mats` |
| `js_cv.cpp` | 391 | `js_array_to` | `argv[0]` | `a` |
| `js_cv.cpp` | 402 | `js_array_to` | `argv[0]` | `a` |
| `js_cv.cpp` | 416 | `js_array_to` | `argv[0]` | `mv` |
| `js_cv.cpp` | 437 | `js_array_to` | `argv[i++]` | `srcs` |
| `js_imgproc.cpp` | 795 | `js_array_to` | `argv[0]` | `images` |

## `Point2fVector` (`std::vector<cv::Point2f>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `Point2fVectorVector` (`std::vector<std::vector<cv::Point2f>>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `Point3fVector` (`std::vector<cv::Point3f>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `PointVector` (`std::vector<cv::Point>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_psimpl.cpp` | 170 | `js_array_to` | `argv[0]` | `points` |

## `PointVectorVector` (`std::vector<std::vector<cv::Point>>`)

No `js_array_(to|from)`/`js_value_(to|from)` argument-conversion sites found for this type. Either nothing in the codebase converts a plain JS array/value into this container this way, or the conversion goes through the `JSVector<T>` class wrapper (`include/js_vector.hpp`) instead of a raw-array marshaling call.

## `RectVector` (`std::vector<cv::Rect>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_dnn.cpp` | 863 | `js_value_to` | `argv[0]` | `rBlob` |
| `js_dnn.cpp` | 877 | `js_value_to` | `argv[0]` | `rBlob` |
| `js_dnn.cpp` | 1753 | `js_value_to` | `argv[1]` | `roiRects` |
| `js_dnn.cpp` | 2274 | `js_value_to` | `argv[0]` | `bboxes` |
| `js_dnn.cpp` | 2603 | `js_array_to` | `argv[0]` | `bboxes` |
| `js_dnn.cpp` | 2641 | `js_value_to` | `argv[0]` | `bboxes` |
| `js_ximgproc.cpp` | 1294 | `js_array_to` | `argv[0]` | `rects` |

## `StringVector` (`std::vector<std::string>`)

| File | Line | Call | Argument | Variable |
|---|---|---|---|---|
| `js_dnn.cpp` | 334 | `js_value_to` | `argv[0]` | `names` |
| `js_dnn.cpp` | 1344 | `js_value_to` | `argv[0]` | `names` |
| `js_dnn.cpp` | 1789 | `js_value_to` | `argv[0]` | `vocabulary` |
| `js_filestorage.cpp` | 354 | `js_array_to` | `argv[1]` | `strv` |

